{
  "name": "@bilibili-tools/ffmpeg",
  "version": "0.0.0",
  "main": "lib/index.js",
  "scripts": {
    "dev": "rimraf lib && babel src --watch --out-dir lib --config-file ../../babel.node.config.js",
    "build": "rimraf lib && babel src --out-dir lib --config-file ../../babel.node.config.js"
  }
}
